# Git LFS tracking for large PsychEngine binaries and native libs
# Note: install Git LFS locally and run `git lfs install` before migrating history.

# Track the main executable and native binaries
games/PsychEngine/export/release/PsychEngine filter=lfs diff=lfs merge=lfs -text
games/PsychEngine/export/release/lime.ndll filter=lfs diff=lfs merge=lfs -text
games/PsychEngine/export/release/*.ndll filter=lfs diff=lfs merge=lfs -text

# Track common large asset types in the release directory (optional)
games/PsychEngine/export/release/**/*.zip filter=lfs diff=lfs merge=lfs -text
games/PsychEngine/export/release/**/*.exe filter=lfs diff=lfs merge=lfs -text
